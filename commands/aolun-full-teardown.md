---
name: aolun-full-teardown
description: |
  对目标帖子执行完整的底朝天拆解流程（Workflow 3），输出深度攻击文。
---

读取并遵循 `skills/aolun-fileflow/SKILL.md`，执行 Workflow 3：底朝天全拆。

目标帖子：
$ARGUMENTS

流程：
01-dissect → 02-scan → 02b-deep-verify（可选）→ 03-other-mountains（可选）→ 04-attack → 99-final

1. `01-dissect`：解剖概念与逻辑。
2. `02-scan`：扫描未覆盖的弱点。
3. `02b-deep-verify`（可选）：仅证据不足维度触发深度验证。
4. `03-other-mountains`（可选）：跨领域解法。
5. `04-attack`：综合攻击文（深度拆解结构）。
6. `99-final`：整合成完整深度技术评论文章。
